services:
  waida:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/database.db
    volumes:
      - waida_db:/data
    restart: unless-stopped

volumes:
  waida_db:

